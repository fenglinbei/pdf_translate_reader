# QA Agent 第一阶段收尾记录

日期：2026-09-24。状态：**第一阶段工程拆分与本地功能验收已收尾；第二阶段仅交付设计。**

## 1. 阶段编号与交付边界

后续统一使用下面的名称，避免把故障排查优先级当成 Agent 升级阶段：

| 名称 | 范围 | 状态 |
| --- | --- | --- |
| P1：执行内核拆分 | 执行循环、模型适配、工具注册、事件输出；版本及 QA 隔离交付规范 | 已实现并完成本地核验 |
| F01：全文读取修复 | 修复应用对象与数据库字段契约不一致；作为 P1 兼容修复交付 | 已部署本地 QA，人工验收通过 |
| P2：工具协议与模型自主查阅文档 | 原生工具调用、参数校验、结果回传；文档阅读工具、缓存、引用与运行记录 | [设计已交付，未实施](qa-agent-stage-2-plan.md) |
| F02：运行记录与失败控制 | 全链路用量、事件序号、取消、回落与终态 | [配套方案](qa-execution-observability-plan.md)，纳入 P2 |
| F03：证据身份与定位 | 稳定证据标识、跨轮坐标、无向量索引时的引用 | 待实现；P2 新路径的必要前置 |

此前文档中的“顺序 2：失败记录与回落边界”指 F02，不是原定 P2 的全部范围。
本次收尾只修改文档，不添加第二阶段运行代码、依赖、数据库迁移、配置开关或新版本标签。

## 2. 冻结的基线

| 项目 | 确切记录 |
| --- | --- |
| 工作分支 | `qa-agent`，远程 `fenglinbei/pdf_translate_reader` |
| 收尾核查起点 | `2bc8bb52692caece8eddacd4e040bb41ed8dd18c`；开始时工作区干净 |
| P1 拆分前基线 | `23ddc3b713b20b34edcceacdb0199da280c39ca0` |
| 执行器拆分提交 | `240a8fe` |
| 已验收运行源码 | `1cc80bdd4cc71871b55d83175dee4ea9eca23804` |
| QA / 应用版本 | `0.1.1-alpha.1` / `0.1.0`，本次不递增 |
| 当前本地 QA | `127.0.0.1:8789`，独立发布目录运行，不随工作区文档变动更新 |
| 生产 | 本轮 Agent 升级未部署生产 |

执行内核接口、阅读顺序和离线示例见 [第一阶段说明](qa-agent-runtime.md)。
发布、隔离与回滚约束见 [版本与交付规范](versioning-and-delivery.md)。

## 3. 本轮完成的核验

- Node.js `24.19.0`，与 `.nvmrc` 一致。
- `npm run ci`：版本校验、**287 项测试全部通过**、TypeScript 与 Vite 构建通过；保留已有的大 chunk 构建提示。
- `npm run check:agent-equivalence`：与拆分前基线的 **7 组轨迹完全一致**，包含返回值、落库入参、事件顺序、模型上下文和检索参数。
- 起初沙箱禁止 `tar` 子进程和临时端口监听，相关测试报 `EPERM`；在允许这些操作的环境中重跑同一检查通过，未改测试或产品代码来绕过失败。
- 只读核对远程：验收源码的 [CI](https://github.com/fenglinbei/pdf_translate_reader/actions/runs/35879493619) 与核查起点的 [CI](https://github.com/fenglinbei/pdf_translate_reader/actions/runs/35882063314) 均成功。
- 本次文档提交对应的检查另由分支 push 触发，以该提交的 CI 为准，不能用上述历史 CI 代替。

本轮健康复核：

| 请求 | 结果 | 能证明的范围 |
| --- | --- | --- |
| QA `8789 /api/qa/health` | 200；版本 `0.1.1-alpha.1`，完整 SHA 为 `1cc80bdd…` | 已验收 QA 进程仍可访问且源码身份正确 |
| 主应用 `8787 /api/health` | 200 | 主应用进程健康 |
| 测试主应用 `8790 /api/health` | 200 | 测试主应用进程健康 |
| 测试主应用 `8790 /api/qa/threads` | 503 | 其内嵌 QA 仍关闭 |

本轮没有重启服务、调用真实模型、写入业务数据库或重新执行人工浏览器验收。
本轮 CI 输出和健康结果位于本机 `/tmp/qa-phase1-closeout-*-20260924.*`，不公开运行环境配置。

## 4. 已完成的人工验收及其范围

2026-09-23 本地部署后，人工反馈为“验收通过了，全文检查正确触发”。
部署后的只读记录确认：一条 `deepseek-flash` 问答以 `long-context` 成功保存，另一条
`deepseek-v4-pro` 问答以混合检索路径成功保存。两条是不同请求，不视为一次回落。
具体时间、统计及修复因果链见 [真实运行核验](qa-local-runs-2026-09-23.md)。

这完成 P1 正常路径及 F01 的验收；不扩展为所有回答事实正确、所有故障场景通过或生产验收。

## 5. 显式移交的未完成项

| 未完成项 | 后续归属与要求 |
| --- | --- |
| 原生工具协议、严格参数校验、工具错误回传模型 | P2 协议闭环；现有 JSON 动作不能算已交付这些升级 |
| 文档可读性仍绑定向量索引 `ready` | P2 解耦服务端与前端条件；无 embedding 配置也应能查阅已解析文档 |
| 路由/全文步骤、终止原因和全调用用量不完整 | F02；必须保留缺失项，不能把未记录用量当成零 |
| 取消、输出后失败、保存失败的统一终止语义 | F02；不能因此再次生成或偷偷改走另一条付费路径 |
| 跨轮 `lineRegions` 丢失、C 编号随重排改变 | F03；P2 采用稳定来源身份，保留页码及可用坐标 |
| 引用表必须关联索引 chunk；工具名有数据库白名单 | P2 增量迁移与兼容读取；不能只增加 JavaScript 工具 |
| `qa-trace.sql` 覆盖传入 msg_id，跨分钟耗时错误 | F02 配套修复；本次不把该脚本视作完整可靠的轨迹证明 |
| 回答质量、缓存命中与成本对照 | P2 固定评测集，分别报告正确性、来源覆盖、延迟和费用 |
| GitHub 保护规则、发布环境、独立生产切流 | 独立发布工作；工作流已入库不等于 CD 环境已配置 |

P1 的完成条件是拆分可读、可测试、行为保持一致，以及已声明的本地正常路径通过。
这些后续能力不回填为 P1 成果；P2 也不要求本次先执行任何新运行时改造。
