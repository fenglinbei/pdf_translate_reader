# 文献页 UI 优化发布记录

**已部署并完成生产浏览器验证。** 服务于 2026-09-23 13:01:51（Asia/Shanghai）启动新版本 `2840e952f9b381a0ea2c3c6aee0a32b41b14af9d`。代码已推送至 `codex/library-metadata-enrichment`；本发布记录随后单独提交，生产代码版本以上述 SHA 为准。

入口：[pdf.fenglin.pro](https://pdf.fenglin.pro)、[pdf-translate-reader.xyz](https://pdf-translate-reader.xyz)。

## 界面调整

- AI 补全开关集中在设置 → 文献信息识别，文献页顶部直接呈现搜索和筛选。
- 单篇重新识别和编辑改为详情右上角的轻量图标，支持悬停提示、键盘焦点和进行中状态。来源说明通过信息图标展开，待确认建议仍可直接逐项比较、勾选。
- 元数据编辑按文献信息、文献编号、分类与阅读组织。标题与作者为可随内容增高的多行输入，作者保留姓名中的逗号。
- 编辑正文独立滚动，底部保存与取消固定可见，支持 Ctrl/Cmd + Enter 保存。错误提示保持可见，失败或版本冲突保留草稿。
- 列表识别状态与最近打开时间并排显示；修正关闭详情后焦点返回导致侧栏重新打开的问题。

本次没有新增迁移、依赖或识别算法变化，继续复用已上线的队列、人工字段保护和独立 AI 偏好。

## 验证

`npm run test:library`、`npm run test:mathpix`、`npm run build`、`git diff --check` 均通过。构建仍有原有的大资源包提示。

本地 Chromium 使用真实界面组件与受控数据完成 34 项检查，包括保存失败、后台更新、版本冲突时保留草稿，滚动后错误可见，取消恢复内容与焦点，逐项建议选择，批量补全，以及 320px、390px、1024px 布局和操作可见性。控制台和页面错误为 0。

生产 Chromium 在 `pdf.fenglin.pro` 使用临时账号和一份合成 PDF 完成 15 项检查：编辑标题和作者、刷新后保留修改、设置中的 AI 开关实际保存到云端并在刷新后保留、识别图标成功排队、后台结果返回待确认建议且保留人工值、建议默认不选中，以及 390px 编辑与关闭操作。控制台和页面错误为 0。

随后通过数据库复核 4 项结果：AI 关闭偏好、保存后的标题和作者数组、人工字段保护、AI 关闭时重新识别保留人工值并生成建议。合成文件、文献记录和账号均已删除。本次识别时 AI 保持关闭，没有新增模型调用；识别准确率与真实资料人工验收仍沿用[首次发布记录](library-metadata-production-2026-09-23.md)的范围。

非敏感机器记录见[发布检查 JSON](library-metadata-ui-production-2026-09-23.json)。

## 部署与运行

部署前备份了应用、依赖、Git 状态、网页目录和 Nginx/systemd 配置，归档可读性检查通过。服务器备份目录为 `/opt/backups/pdf_translate_reader/20260923T050008Z-library-ui`。随后通过原部署脚本完成构建、静态发布和服务重启。

API 服务与 Nginx 均正常，API 重启计数为 0。两个域名的健康接口均为 HTTP 200，未登录元数据请求均为 HTTP 401；HTML、主 JS、CSS 的 SHA-256 均与生产构建一致。

生产环境文件、服务器手工修改、历史备份文件和 Nginx/systemd 配置通过哈希保留检查。构建用的临时交换空间已移除。主资源未包含服务端私有配置。

主 JS 为 `/assets/index-BuBVV2qF.js`，SHA-256 为 `4378a5d5812dc55daee85179f436ca1722164bd0fd47f8d54ba6d1df83157f7a`。
