# 2026-09-23 本地 QA 两轮真实运行核验

当前状态：**初始两轮 Agent 回答成功落库；修复后的全文路径已通过本地人工验收。** 本记录不代表生产部署或回答事实准确性的全面验收。

后续更新：全文读取缺陷已在 `0.1.1-alpha.1` 修复，已部署本地验收服务，实际读取、合成路由测试及全文路径人工验收通过；见文末。
以下两轮记录保留当时证据范围，不能将修复后的结果追认为历史运行结果。

## 证据与范围

- 2026-09-23 22:16（Asia/Shanghai）只读核查 `.env.qa.local` 指向的独立测试项目；已确认它与主应用配置的 Supabase 项目不同。
- 当天查到两条助手消息、两条提问、13 个步骤、3 次工具调用、8 条 API 日志、11 条引用。本文不包含账号、项目地址、消息 UUID、论文正文或答案全文。
- 原始核验快照保留在本地忽略目录 `output/qa-local-runs-2026-09-23.private.json`；统计与跨轮证据对应关系另存同目录的 `.summary.json`。
- 代码核查基线为 `qa-agent` 的 `8aacaaa`。运行服务报告 `sha=development`，没有每次请求的源码 SHA，不能把该提交当作两轮运行的确切部署版本。
- 第二轮的路由日志片段此前记录于 `docs/qa-request-flow.md`（提交 `8aacaaa`）；本次用数据库记录和当前控制流交叉核对，没有重新发起模型请求。

## 两轮结果

两轮均使用 `deepseek-flash`、`reasoningEffort=deep`、`qa-answer-v2`、`agent-runner-v1`。
索引状态为 `ready`，来源为 `mathpix-v3-pdf`；实际使用 `voyage-4-large` 的 1024 维语义检索与 `rerank-2.5` 重排。

| 项目 | 第一轮：指标与 SOTA 差距 | 第二轮：文章主要内容 |
| --- | --- | --- |
| 服务端请求区间（北京时间） | 21:37:03.995–21:37:37.749 | 21:45:40.512–21:46:06.810 |
| 最终消息状态 | `success` | `success` |
| 实际完成路径 | Agent 检索 → 打开证据 → 回答 | 日志显示 global → 长上下文尝试 → 回落 Agent → 回答 |
| 上轮带入证据 | 0 | 2 |
| 工具 | 1 次搜索、1 次 `open_chunk` | 1 次搜索，无 `open_chunk` |
| 搜索候选数 | 8，合并去重后 7 | 8，加上带入证据后 10 |
| 最终证据数 | 2 | 10 |
| 落库步骤数 | 8 | 5 |
| 引用数 | 2，全部 `verified` | 9，全部 `verified` |
| 引用拒绝数 / 警告 | 0 / 无 | 0 / 无 |
| 服务端请求耗时 | 33.754 秒 | 26.298 秒 |
| 搜索工具记录耗时 | 20.141 秒 | 4.469 秒 |
| 答案生成记录的 token | 输入 3213，输出 653，总计 3866 | 输入 12295，输出 1537，总计 13832 |

第一轮时间线：`plan → gap_check(search) → tool_call → observation → gap_check(open) → tool_call → observation → answer_outline`。

第二轮时间线：`plan（带入 2 条证据）→ gap_check(search) → tool_call → observation → answer_outline`。

这里的耗时来自 `answer-stream` 日志起止时间，不是浏览器首字延迟；token 仅是最终答案调用的记录，
不包括路由器、控制器、embedding 等整条链路的全部消耗。`rerank` 日志复用了检索起点，不能把各类日志耗时直接相加。
`verified` 是现有引用校验器的结果，不能替代对论文数值、推论与答案完整性的人工审阅。

## 需要纠正的运行解读

1. **两轮最终走 Agent，不等于两轮都发生回落。** 第一轮只能确认 Agent 路径完成；没有保存它的路由日志，不能断言它先进入过长上下文。第二轮已有 `global/high`、进入长上下文及后续 Agent 日志片段，结合最终 `hybrid-retriever-v1` 快照，可确认发生回落。具体异常尚无持久化证据。
2. **带入证据不证明紧邻上一轮的路由。** `findLatestCarryoverEvidence()` 会向前寻找同一文档最近一次非空证据快照，中间即使有长上下文回答也会被跳过。本次两条证据可以直接按 chunk 对应到第一轮；“两轮都回落、系统性失败”的原结论撤回。
3. **`queryPlan.intent=question` 不是路由器输出。** 数据库中两轮都是这个值，它来自规则式查询规划，不能据此否定第二轮日志里的 `global`。
4. **控制器次数没有独立落库。** 两轮分别有 2 / 1 个 `gap_check`，并有非空结束大纲。按当前控制流推断为 3 / 2 次控制器调用，但这不是持久化计数；“gap_check 总是等于调用次数减一”不成立。

## 本轮确认的问题与后续顺序

| 顺序 | 问题 | 证据 / 下一步 |
| --- | --- | --- |
| 1 | 长上下文失败缺少可追溯原因 | 第二轮最终成功掩盖了中间失败；8 条 API 日志均为 success，`fallback` 只发 SSE。先持久化路由、尝试路径、回落原因与终止动作，再复现并定位长上下文失败。 |
| 2 | 跨轮带入证据丢失高亮坐标 | 第一轮 C1/C2 的 `lineRegions` 分别有 22/105 项；同一 chunk 在第二轮 C9/C10 均为 0。`normalizeCarryoverEvidence()` 的字段映射遗漏此项。记录为待修复，尚未复测其浏览器表现。 |
| 3 | 引用编号只在某次快照内有效 | 同一 chunk 从第一轮 C1/C2 变为第二轮 C9/C10；plan 时带入证据也先经过归一化。轨迹应记录稳定 chunk 身份和每次编号映射，不能仅按 C 号串联。 |
| 4 | 终止决策与完整成本不可观测 | `finish_retrieval` 在写步骤之前退出，diagnostics 也没有实际 controller 调用次数；路由/控制器 usage 未统一累计。补充可核对的动作与用量记录。 |

另有轨迹工具限制：`scripts/qa-trace.sql` 无条件 `\set msg_id ''` 会覆盖 `psql -v msg_id=...`，
而 `extract(milliseconds from interval)` 不能表达跨分钟的总毫秒数。本次核验使用按消息 ID 过滤的只读查询和完整时间差，未依赖这两处输出。脚本修复列入下一次开发。

## 验收状态更新

- 已确认：隔离测试项目已配置；解析产物与索引可用；真实模型、检索、打开证据、跨轮带入、答案与引用落库已执行。
- 当前健康检查：QA 8789 返回 200（`0.1.0-alpha.1 / development`）；测试主应用 8790 返回 200，其内嵌 QA 返回 `503 qa_disabled`。
- 本次核验时 5173/5174 均未响应；未重新打开浏览器，没有重放历史 SSE 或确认当前前端登录态。
- 尚未通过：长上下文成功回答；回落异常根因；高亮坐标修复；答案事实准确性的人工验收；生产部署与切流。

上述初次同步仅记录与纠正结论，没有修改执行器、测试数据或运行中的服务。

## 后续定位与修复：0.1.1-alpha.1

当前代码的只读复现返回 `invalid input syntax for type uuid: "undefined"`。
原因是 `getLatestQaIndexJob()` 返回 cloudDocumentId/contentSha256 应用对象，
而 MathPix 读取器仍取 user_id/user_document_id/content_sha256 数据库字段；传入对象也不包含 user_id。
索引构建传原始行，因此此前索引成功不能证明全文问答可用。该不匹配在执行器拆分前已存在。

修复将读取器改为显式接收 userId/userDocumentId/contentSha256，两处调用各自映射，
并在访问数据库前拒绝缺失、非字符串或空白参数。继续保留用户、文档、内容哈希、completed 与未删除过滤。

验证记录：

- 旧代码新增回归先失败，捕获查询中的 undefined；修复后通过。
- 9 项新增回归包含 global 路由：真实路由代码使用合成数据库与模型响应，只发生分类和全文回答两次模型调用，保存 long-context 结果且无 fallback。
- 完整本地 CI：287 项测试、版本校验、TypeScript 和前端构建通过。
- 2026-09-23 23:03（Asia/Shanghai），修复后的 `loadCurrentPaperFullText()` 从同一独立测试文档读取 47,551 字符，未截断，与此前 9 页全文的对照长度一致。
- 实际复核仅 6 次 GET，没有数据库写入或真实模型调用。脱敏结果保存在忽略目录 `output/qa-fulltext-fix-verification-2026-09-23.json`；没有重启现有服务。

边界：这确认了当前读取缺陷与修复，不补造第二轮已经丢失的历史异常。
在该次只读复核时，真实模型的全文回答、浏览器展示和生产部署仍未验收；原有行坐标丢失问题尚未修改。
顺序 2 的持久化轨迹、回落边界、计时与终止处理见 [实施准备](qa-execution-observability-plan.md)。

## 本地部署与全文路径人工验收通过

- 验收源码：`1cc80bdd4cc71871b55d83175dee4ea9eca23804`，QA `0.1.1-alpha.1`。
- 2026-09-23 23:18（Asia/Shanghai）从打包制品启动本地 QA 8789，健康接口返回上述完整 SHA；HTTPS 测试前端 5174 代理一致。原主应用和前端进程未重启。
- 人工反馈：“验收通过了，全文检查正确触发”。范围为这次本地全文路径功能验收。
- 23:27 只读核对部署后的消息与日志：

| 消息开始时间（北京时间） | 模型 | 最终消息状态 | 实际结果 |
| --- | --- | --- | --- |
| 23:22:48 | deepseek-flash | success | `retrieverVersion=long-context`，evidence 为空，存在 `longContext=true` 的成功答案日志 |
| 23:24:54 | deepseek-v4-pro | success | `retrieverVersion=hybrid-retriever-v1`，6 条证据 |

全文路径已具有真实模型成功记录；另一轮检索路径也成功落库，不将两次提问解释为同一次请求的回落。
完整部署信息与脱敏验收结果保留在本地忽略目录的 `qa-local-server/deployment.json`、
`qa-fulltext-owner-acceptance-2026-09-23.json` 中。本次核查没有调用模型或写入测试数据库。

顺序 1 至此完成：代码修复、回归、独立测试部署、全文触发人工验收及成功落库核对均已有证据。
顺序 2 仍为准备阶段，尚未实现；生产部署、跨轮高亮修复、故障矩阵与回答事实准确性的全面验收不在本次通过范围。
