# 文献库自动识别生产发布记录

**已部署并完成生产合成文献验证。** 服务于 2026-09-23 11:52:30（Asia/Shanghai）切换到 `1af6a233ac043bc0a1f7abefdfd87640e91b7125`，功能提交已推送至 `codex/library-metadata-enrichment`。本记录随后单独提交，运行代码版本以上述 SHA 为准。

这是首次识别功能发布的历史记录；同日后续发布的设置入口与编辑界面调整见[文献页 UI 优化发布记录](library-metadata-ui-production-2026-09-23.md)。

入口：[pdf.fenglin.pro](https://pdf.fenglin.pro)、[pdf-translate-reader.xyz](https://pdf-translate-reader.xyz)。

## 发布范围

- 新导入文献自动进入后台识别队列，历史文献由手动批量补全触发。
- PDF 内置信息、首页文字和文献编号查询不足时，使用已接入的 `deepseek-flash` 提取。文献库和设置中提供同一 AI 开关。
- 自动填写空字段或明确的文件名占位；人工修改、人工清空及已有不同值受保护，通过逐项确认采纳建议。
- 后台状态、字段来源、任务恢复、编辑版本检查，以及文献库与未手改翻译上下文的同步已启用。

具体触发和覆盖规则见[功能说明](library-metadata-recognition.md)。

## 迁移与部署

项目所有者在 Supabase SQL 编辑器执行了 `supabase/migrations/20260923_library_metadata.sql`。发布前经生产 REST 接口复核，文献上的 3 个元数据字段、设置上的独立 AI 开关及 5 个 RPC 均已就绪。

发布前完整备份了应用、依赖、Git 状态、环境文件、网页目录、Nginx/systemd 配置及服务器手工补丁；备份归档通过可读性检查。通过项目原有部署脚本安装依赖、完成生产 TypeScript/Vite 构建、发布静态文件并重启服务。构建用的临时交换空间已关闭并删除。

生产原有环境文件、手工修改和历史文件，以及 Nginx/systemd 配置均通过 SHA-256 保留检查。回滚所需备份保存在服务器受限目录，本次未执行回滚演练。回退应用可以保留新增列和 RPC；若要停止新文献排队，需要同时停用队列触发器。

## 验证结果

发布前 57 项文献库检查、模型/翻译/QA/MathPix 回归及构建通过。生产结果如下，非敏感机器记录见[发布检查 JSON](library-metadata-production-2026-09-23.json)。

| 检查 | 结果 |
| --- | --- |
| API 服务 / Nginx | 均 active；重启计数 0；Nginx 配置检查通过 |
| 两个公网健康接口 | 均 HTTP 200 |
| 公网 HTML、主 JS、CSS | 两个域名均 HTTP 200，SHA-256 与生产构建及发布目录一致 |
| 未登录的元数据补全请求 | 两个域名均 HTTP 401 |
| 隔离 Chromium 登录页 | 两个域名正常，控制台错误/警告、页面错误和失败请求均为 0 |
| 服务配置与手工改动 | 与备份一致；公开入口和主资源未检出服务端密钥或私有模型地址 |
| 合成文献生产 API 链路 | 下述 12 项检查全部通过；测试文件、文献记录和账号已清理 |

生产链路使用临时测试账号登录并上传两份合成 PDF，经过实际 Supabase Storage、文献表、API、后台 worker 和已配置模型：

1. 关闭 AI 后，旧客户端形态的普通设置保存仍保留关闭状态。
2. 关闭 AI 时，新导入记录自动排队。
3. 实际 PDF 中的题名和有序作者数组成功写回。
4. 关闭 AI 时未调用模型，PDF 信息识别正常完成。
5. 登录后的批量补全 API 成功排队。
6. 手工编辑题名后再识别，原值保持且字段受保护。
7. 不同识别结果进入待确认建议。
8. 仅采用选中的题名建议，更新后继续保护该字段。
9. 启用 AI 时，新导入记录自动排队。
10. 缺少元数据时实际调用模型兜底。
11. 模型提取的题名、两位作者和年份均与合成原文一致。
12. 字段的 AI 来源正确保存。

真实模型调用为 `deepseek-flash`，总用量 292 tokens（输入 184、输出 108）。合成链路于 11:53:17–11:53:31 完成。没有使用用户真实文献。

主 JS 为 `/assets/index-DtIAwzSf.js`，SHA-256 为 `14996579c9e5cadb093c108e4967808d425b58afd2fbb0027624aba536b35eea`。

浏览器检查覆盖公开登录页；登录后的真实文献上传、批量操作效果和识别准确率未在本次人工验收。上述合成 API 链路验证不代替真实资料验收或大批量吞吐量评估。
