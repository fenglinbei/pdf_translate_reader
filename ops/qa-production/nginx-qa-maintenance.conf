# Temporary QA-only maintenance while draining old requests and migrating history.
location ^~ /api/qa/ {
  default_type application/json;
  add_header Retry-After 60 always;
  return 503 '{"error":{"code":"qa_maintenance","message":"问答服务正在升级，请稍后重试。"}}';
}
